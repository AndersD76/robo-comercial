[phases.setup]
nixPkgs = ["chromium", "chromium-driver", "xvfb-run"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install --with-deps chromium chromium-headless-shell"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
