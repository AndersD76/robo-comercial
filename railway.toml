[build]
buildCommand = "pip install -r requirements.txt && PLAYWRIGHT_BROWSERS_PATH=/app/ms-playwright playwright install --with-deps chromium"

[deploy]
startCommand = "PLAYWRIGHT_BROWSERS_PATH=/app/ms-playwright PLAYWRIGHT_CHROMIUM_USE_HEADLESS_NEW=0 gunicorn app:app"
